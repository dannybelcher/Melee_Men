using UnityEngine;

/// <summary>
/// Defines the <see cref="MusicStop" />
/// </summary>
public class MusicStop : MonoBehaviour
{
    // Start is called before the first frame update
    /// <summary>
    /// The Start
    /// </summary>
    internal void Start()
    {
    }

    // Update is called once per frame
    /// <summary>
    /// The Update
    /// </summary>
    internal void Update()
    {
    }
}
